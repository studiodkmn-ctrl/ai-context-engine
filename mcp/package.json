{
  "name": "ai-context-mcp",
  "version": "0.1.0",
  "description": "Schlanke MCP-Schicht über AI Context Pro — gezieltes Gedächtnis für jeden Agenten.",
  "type": "module",
  "bin": {
    "ai-context-mcp": "dist/server.js"
  },
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0 || ^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
